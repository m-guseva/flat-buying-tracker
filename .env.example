DATABASE_URL="file:../data/app.db"

# Set to "true" to make this instance view-only (e.g. a shared Codespace):
# blocks every create/update/delete action, both server-side and in the UI.
# NEXT_PUBLIC_READ_ONLY=true
